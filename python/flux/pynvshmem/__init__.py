# DON'T EDIT THIS FILE. AUTOMATICALLY GENERATED BY autogen_pybind.py
import ctypes
import torch
import builtins
import numpy as np
from typing import Any, List, Sequence, Tuple, Union
import flux
from flux_ths_pybind._pynvshmem import *

from ._wrapper import *

# fmt: off

def pynvshmemx_float_alltoall_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nelem: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_float_alltoall_on_stream(team, dest, src, nelem, _stream_raw(stream))


def pynvshmemx_double_alltoall_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nelem: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_double_alltoall_on_stream(team, dest, src, nelem, _stream_raw(stream))


def pynvshmemx_char_alltoall_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nelem: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_char_alltoall_on_stream(team, dest, src, nelem, _stream_raw(stream))


def pynvshmemx_short_alltoall_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nelem: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_short_alltoall_on_stream(team, dest, src, nelem, _stream_raw(stream))


def pynvshmemx_schar_alltoall_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nelem: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_schar_alltoall_on_stream(team, dest, src, nelem, _stream_raw(stream))


def pynvshmemx_int_alltoall_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nelem: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_int_alltoall_on_stream(team, dest, src, nelem, _stream_raw(stream))


def pynvshmemx_long_alltoall_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nelem: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_long_alltoall_on_stream(team, dest, src, nelem, _stream_raw(stream))


def pynvshmemx_longlong_alltoall_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nelem: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_longlong_alltoall_on_stream(team, dest, src, nelem, _stream_raw(stream))


def pynvshmemx_uchar_alltoall_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nelem: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_uchar_alltoall_on_stream(team, dest, src, nelem, _stream_raw(stream))


def pynvshmemx_ushort_alltoall_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nelem: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_ushort_alltoall_on_stream(team, dest, src, nelem, _stream_raw(stream))


def pynvshmemx_uint_alltoall_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nelem: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_uint_alltoall_on_stream(team, dest, src, nelem, _stream_raw(stream))


def pynvshmemx_ulong_alltoall_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nelem: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_ulong_alltoall_on_stream(team, dest, src, nelem, _stream_raw(stream))


def pynvshmemx_ulonglong_alltoall_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nelem: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_ulonglong_alltoall_on_stream(team, dest, src, nelem, _stream_raw(stream))


def pynvshmemx_int8_alltoall_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nelem: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_int8_alltoall_on_stream(team, dest, src, nelem, _stream_raw(stream))


def pynvshmemx_int16_alltoall_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nelem: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_int16_alltoall_on_stream(team, dest, src, nelem, _stream_raw(stream))


def pynvshmemx_int32_alltoall_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nelem: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_int32_alltoall_on_stream(team, dest, src, nelem, _stream_raw(stream))


def pynvshmemx_int64_alltoall_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nelem: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_int64_alltoall_on_stream(team, dest, src, nelem, _stream_raw(stream))


def pynvshmemx_uint8_alltoall_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nelem: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_uint8_alltoall_on_stream(team, dest, src, nelem, _stream_raw(stream))


def pynvshmemx_uint16_alltoall_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nelem: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_uint16_alltoall_on_stream(team, dest, src, nelem, _stream_raw(stream))


def pynvshmemx_uint32_alltoall_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nelem: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_uint32_alltoall_on_stream(team, dest, src, nelem, _stream_raw(stream))


def pynvshmemx_uint64_alltoall_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nelem: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_uint64_alltoall_on_stream(team, dest, src, nelem, _stream_raw(stream))


def pynvshmemx_size_alltoall_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nelem: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_size_alltoall_on_stream(team, dest, src, nelem, _stream_raw(stream))


def pynvshmemx_ptrdiff_alltoall_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nelem: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_ptrdiff_alltoall_on_stream(team, dest, src, nelem, _stream_raw(stream))


def pynvshmemx_alltoallmem_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nelem: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_alltoallmem_on_stream(team, dest, src, nelem, _stream_raw(stream))


def pynvshmemx_barrier_on_stream(team: np.intc, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_barrier_on_stream(team, _stream_raw(stream))


def pynvshmemx_barrier_all_on_stream(stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_barrier_all_on_stream(_stream_raw(stream))


def pynvshmemx_team_sync_on_stream(team: np.intc, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_team_sync_on_stream(team, _stream_raw(stream))


def pynvshmemx_sync_all_on_stream(stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_sync_all_on_stream(_stream_raw(stream))


def pynvshmemx_float_broadcast_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nelem: np.uint, PE_root: np.intc, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_float_broadcast_on_stream(team, dest, src, nelem, PE_root, _stream_raw(stream))


def pynvshmemx_double_broadcast_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nelem: np.uint, PE_root: np.intc, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_double_broadcast_on_stream(team, dest, src, nelem, PE_root, _stream_raw(stream))


def pynvshmemx_char_broadcast_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nelem: np.uint, PE_root: np.intc, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_char_broadcast_on_stream(team, dest, src, nelem, PE_root, _stream_raw(stream))


def pynvshmemx_short_broadcast_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nelem: np.uint, PE_root: np.intc, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_short_broadcast_on_stream(team, dest, src, nelem, PE_root, _stream_raw(stream))


def pynvshmemx_schar_broadcast_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nelem: np.uint, PE_root: np.intc, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_schar_broadcast_on_stream(team, dest, src, nelem, PE_root, _stream_raw(stream))


def pynvshmemx_int_broadcast_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nelem: np.uint, PE_root: np.intc, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_int_broadcast_on_stream(team, dest, src, nelem, PE_root, _stream_raw(stream))


def pynvshmemx_long_broadcast_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nelem: np.uint, PE_root: np.intc, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_long_broadcast_on_stream(team, dest, src, nelem, PE_root, _stream_raw(stream))


def pynvshmemx_longlong_broadcast_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nelem: np.uint, PE_root: np.intc, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_longlong_broadcast_on_stream(team, dest, src, nelem, PE_root, _stream_raw(stream))


def pynvshmemx_uchar_broadcast_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nelem: np.uint, PE_root: np.intc, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_uchar_broadcast_on_stream(team, dest, src, nelem, PE_root, _stream_raw(stream))


def pynvshmemx_ushort_broadcast_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nelem: np.uint, PE_root: np.intc, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_ushort_broadcast_on_stream(team, dest, src, nelem, PE_root, _stream_raw(stream))


def pynvshmemx_uint_broadcast_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nelem: np.uint, PE_root: np.intc, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_uint_broadcast_on_stream(team, dest, src, nelem, PE_root, _stream_raw(stream))


def pynvshmemx_ulong_broadcast_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nelem: np.uint, PE_root: np.intc, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_ulong_broadcast_on_stream(team, dest, src, nelem, PE_root, _stream_raw(stream))


def pynvshmemx_ulonglong_broadcast_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nelem: np.uint, PE_root: np.intc, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_ulonglong_broadcast_on_stream(team, dest, src, nelem, PE_root, _stream_raw(stream))


def pynvshmemx_int8_broadcast_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nelem: np.uint, PE_root: np.intc, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_int8_broadcast_on_stream(team, dest, src, nelem, PE_root, _stream_raw(stream))


def pynvshmemx_int16_broadcast_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nelem: np.uint, PE_root: np.intc, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_int16_broadcast_on_stream(team, dest, src, nelem, PE_root, _stream_raw(stream))


def pynvshmemx_int32_broadcast_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nelem: np.uint, PE_root: np.intc, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_int32_broadcast_on_stream(team, dest, src, nelem, PE_root, _stream_raw(stream))


def pynvshmemx_int64_broadcast_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nelem: np.uint, PE_root: np.intc, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_int64_broadcast_on_stream(team, dest, src, nelem, PE_root, _stream_raw(stream))


def pynvshmemx_uint8_broadcast_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nelem: np.uint, PE_root: np.intc, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_uint8_broadcast_on_stream(team, dest, src, nelem, PE_root, _stream_raw(stream))


def pynvshmemx_uint16_broadcast_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nelem: np.uint, PE_root: np.intc, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_uint16_broadcast_on_stream(team, dest, src, nelem, PE_root, _stream_raw(stream))


def pynvshmemx_uint32_broadcast_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nelem: np.uint, PE_root: np.intc, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_uint32_broadcast_on_stream(team, dest, src, nelem, PE_root, _stream_raw(stream))


def pynvshmemx_uint64_broadcast_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nelem: np.uint, PE_root: np.intc, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_uint64_broadcast_on_stream(team, dest, src, nelem, PE_root, _stream_raw(stream))


def pynvshmemx_size_broadcast_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nelem: np.uint, PE_root: np.intc, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_size_broadcast_on_stream(team, dest, src, nelem, PE_root, _stream_raw(stream))


def pynvshmemx_ptrdiff_broadcast_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nelem: np.uint, PE_root: np.intc, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_ptrdiff_broadcast_on_stream(team, dest, src, nelem, PE_root, _stream_raw(stream))


def pynvshmemx_float_fcollect_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nelem: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_float_fcollect_on_stream(team, dest, src, nelem, _stream_raw(stream))


def pynvshmemx_double_fcollect_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nelem: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_double_fcollect_on_stream(team, dest, src, nelem, _stream_raw(stream))


def pynvshmemx_char_fcollect_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nelem: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_char_fcollect_on_stream(team, dest, src, nelem, _stream_raw(stream))


def pynvshmemx_short_fcollect_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nelem: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_short_fcollect_on_stream(team, dest, src, nelem, _stream_raw(stream))


def pynvshmemx_schar_fcollect_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nelem: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_schar_fcollect_on_stream(team, dest, src, nelem, _stream_raw(stream))


def pynvshmemx_int_fcollect_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nelem: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_int_fcollect_on_stream(team, dest, src, nelem, _stream_raw(stream))


def pynvshmemx_long_fcollect_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nelem: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_long_fcollect_on_stream(team, dest, src, nelem, _stream_raw(stream))


def pynvshmemx_longlong_fcollect_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nelem: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_longlong_fcollect_on_stream(team, dest, src, nelem, _stream_raw(stream))


def pynvshmemx_uchar_fcollect_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nelem: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_uchar_fcollect_on_stream(team, dest, src, nelem, _stream_raw(stream))


def pynvshmemx_ushort_fcollect_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nelem: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_ushort_fcollect_on_stream(team, dest, src, nelem, _stream_raw(stream))


def pynvshmemx_uint_fcollect_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nelem: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_uint_fcollect_on_stream(team, dest, src, nelem, _stream_raw(stream))


def pynvshmemx_ulong_fcollect_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nelem: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_ulong_fcollect_on_stream(team, dest, src, nelem, _stream_raw(stream))


def pynvshmemx_ulonglong_fcollect_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nelem: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_ulonglong_fcollect_on_stream(team, dest, src, nelem, _stream_raw(stream))


def pynvshmemx_int8_fcollect_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nelem: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_int8_fcollect_on_stream(team, dest, src, nelem, _stream_raw(stream))


def pynvshmemx_int16_fcollect_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nelem: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_int16_fcollect_on_stream(team, dest, src, nelem, _stream_raw(stream))


def pynvshmemx_int32_fcollect_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nelem: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_int32_fcollect_on_stream(team, dest, src, nelem, _stream_raw(stream))


def pynvshmemx_int64_fcollect_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nelem: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_int64_fcollect_on_stream(team, dest, src, nelem, _stream_raw(stream))


def pynvshmemx_uint8_fcollect_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nelem: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_uint8_fcollect_on_stream(team, dest, src, nelem, _stream_raw(stream))


def pynvshmemx_uint16_fcollect_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nelem: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_uint16_fcollect_on_stream(team, dest, src, nelem, _stream_raw(stream))


def pynvshmemx_uint32_fcollect_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nelem: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_uint32_fcollect_on_stream(team, dest, src, nelem, _stream_raw(stream))


def pynvshmemx_uint64_fcollect_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nelem: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_uint64_fcollect_on_stream(team, dest, src, nelem, _stream_raw(stream))


def pynvshmemx_size_fcollect_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nelem: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_size_fcollect_on_stream(team, dest, src, nelem, _stream_raw(stream))


def pynvshmemx_ptrdiff_fcollect_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nelem: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_ptrdiff_fcollect_on_stream(team, dest, src, nelem, _stream_raw(stream))


def pynvshmemx_uchar_and_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_uchar_and_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_ushort_and_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_ushort_and_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_uint_and_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_uint_and_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_ulong_and_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_ulong_and_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_ulonglong_and_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_ulonglong_and_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_int8_and_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_int8_and_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_int16_and_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_int16_and_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_int32_and_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_int32_and_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_int64_and_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_int64_and_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_uint8_and_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_uint8_and_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_uint16_and_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_uint16_and_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_uint32_and_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_uint32_and_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_uint64_and_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_uint64_and_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_size_and_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_size_and_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_uchar_or_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_uchar_or_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_ushort_or_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_ushort_or_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_uint_or_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_uint_or_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_ulong_or_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_ulong_or_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_ulonglong_or_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_ulonglong_or_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_int8_or_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_int8_or_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_int16_or_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_int16_or_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_int32_or_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_int32_or_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_int64_or_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_int64_or_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_uint8_or_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_uint8_or_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_uint16_or_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_uint16_or_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_uint32_or_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_uint32_or_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_uint64_or_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_uint64_or_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_size_or_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_size_or_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_uchar_xor_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_uchar_xor_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_ushort_xor_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_ushort_xor_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_uint_xor_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_uint_xor_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_ulong_xor_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_ulong_xor_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_ulonglong_xor_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_ulonglong_xor_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_int8_xor_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_int8_xor_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_int16_xor_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_int16_xor_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_int32_xor_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_int32_xor_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_int64_xor_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_int64_xor_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_uint8_xor_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_uint8_xor_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_uint16_xor_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_uint16_xor_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_uint32_xor_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_uint32_xor_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_uint64_xor_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_uint64_xor_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_size_xor_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_size_xor_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_uchar_max_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_uchar_max_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_ushort_max_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_ushort_max_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_uint_max_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_uint_max_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_ulong_max_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_ulong_max_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_ulonglong_max_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_ulonglong_max_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_int8_max_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_int8_max_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_int16_max_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_int16_max_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_int32_max_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_int32_max_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_int64_max_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_int64_max_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_uint8_max_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_uint8_max_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_uint16_max_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_uint16_max_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_uint32_max_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_uint32_max_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_uint64_max_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_uint64_max_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_size_max_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_size_max_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_char_max_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_char_max_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_schar_max_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_schar_max_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_short_max_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_short_max_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_int_max_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_int_max_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_long_max_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_long_max_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_longlong_max_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_longlong_max_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_float_max_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_float_max_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_double_max_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_double_max_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_uchar_min_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_uchar_min_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_ushort_min_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_ushort_min_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_uint_min_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_uint_min_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_ulong_min_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_ulong_min_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_ulonglong_min_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_ulonglong_min_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_int8_min_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_int8_min_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_int16_min_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_int16_min_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_int32_min_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_int32_min_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_int64_min_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_int64_min_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_uint8_min_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_uint8_min_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_uint16_min_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_uint16_min_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_uint32_min_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_uint32_min_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_uint64_min_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_uint64_min_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_size_min_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_size_min_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_char_min_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_char_min_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_schar_min_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_schar_min_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_short_min_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_short_min_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_int_min_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_int_min_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_long_min_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_long_min_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_longlong_min_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_longlong_min_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_float_min_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_float_min_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_double_min_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_double_min_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_uchar_sum_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_uchar_sum_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_ushort_sum_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_ushort_sum_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_uint_sum_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_uint_sum_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_ulong_sum_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_ulong_sum_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_ulonglong_sum_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_ulonglong_sum_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_int8_sum_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_int8_sum_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_int16_sum_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_int16_sum_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_int32_sum_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_int32_sum_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_int64_sum_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_int64_sum_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_uint8_sum_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_uint8_sum_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_uint16_sum_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_uint16_sum_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_uint32_sum_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_uint32_sum_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_uint64_sum_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_uint64_sum_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_size_sum_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_size_sum_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_char_sum_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_char_sum_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_schar_sum_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_schar_sum_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_short_sum_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_short_sum_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_int_sum_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_int_sum_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_long_sum_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_long_sum_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_longlong_sum_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_longlong_sum_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_float_sum_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_float_sum_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_double_sum_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_double_sum_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_uchar_prod_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_uchar_prod_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_ushort_prod_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_ushort_prod_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_uint_prod_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_uint_prod_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_ulong_prod_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_ulong_prod_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_ulonglong_prod_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_ulonglong_prod_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_int8_prod_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_int8_prod_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_int16_prod_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_int16_prod_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_int32_prod_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_int32_prod_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_int64_prod_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_int64_prod_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_uint8_prod_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_uint8_prod_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_uint16_prod_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_uint16_prod_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_uint32_prod_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_uint32_prod_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_uint64_prod_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_uint64_prod_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_size_prod_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_size_prod_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_char_prod_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_char_prod_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_schar_prod_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_schar_prod_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_short_prod_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_short_prod_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_int_prod_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_int_prod_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_long_prod_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_long_prod_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_longlong_prod_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_longlong_prod_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_float_prod_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_float_prod_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_double_prod_reduce_on_stream(team: np.intc, dest: ctypes.c_void_p, src: ctypes.c_void_p, nreduce: np.uint, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_double_prod_reduce_on_stream(team, dest, src, nreduce, _stream_raw(stream))


def pynvshmemx_float_p_on_stream(dest: ctypes.c_void_p, value: np.float32, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_float_p_on_stream(dest, value, pe, _stream_raw(stream))


def pynvshmemx_double_p_on_stream(dest: ctypes.c_void_p, value: np.float64, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_double_p_on_stream(dest, value, pe, _stream_raw(stream))


def pynvshmemx_char_p_on_stream(dest: ctypes.c_void_p, value: np.char, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_char_p_on_stream(dest, value, pe, _stream_raw(stream))


def pynvshmemx_short_p_on_stream(dest: ctypes.c_void_p, value: np.short, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_short_p_on_stream(dest, value, pe, _stream_raw(stream))


def pynvshmemx_schar_p_on_stream(dest: ctypes.c_void_p, value: np.char, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_schar_p_on_stream(dest, value, pe, _stream_raw(stream))


def pynvshmemx_int_p_on_stream(dest: ctypes.c_void_p, value: np.intc, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_int_p_on_stream(dest, value, pe, _stream_raw(stream))


def pynvshmemx_long_p_on_stream(dest: ctypes.c_void_p, value: np.int_, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_long_p_on_stream(dest, value, pe, _stream_raw(stream))


def pynvshmemx_longlong_p_on_stream(dest: ctypes.c_void_p, value: np.longlong, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_longlong_p_on_stream(dest, value, pe, _stream_raw(stream))


def pynvshmemx_uchar_p_on_stream(dest: ctypes.c_void_p, value: np.uint8, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_uchar_p_on_stream(dest, value, pe, _stream_raw(stream))


def pynvshmemx_ushort_p_on_stream(dest: ctypes.c_void_p, value: np.ushort, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_ushort_p_on_stream(dest, value, pe, _stream_raw(stream))


def pynvshmemx_uint_p_on_stream(dest: ctypes.c_void_p, value: np.uintc, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_uint_p_on_stream(dest, value, pe, _stream_raw(stream))


def pynvshmemx_ulong_p_on_stream(dest: ctypes.c_void_p, value: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_ulong_p_on_stream(dest, value, pe, _stream_raw(stream))


def pynvshmemx_ulonglong_p_on_stream(dest: ctypes.c_void_p, value: np.ulonglong, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_ulonglong_p_on_stream(dest, value, pe, _stream_raw(stream))


def pynvshmemx_int8_p_on_stream(dest: ctypes.c_void_p, value: np.char, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_int8_p_on_stream(dest, value, pe, _stream_raw(stream))


def pynvshmemx_int16_p_on_stream(dest: ctypes.c_void_p, value: np.short, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_int16_p_on_stream(dest, value, pe, _stream_raw(stream))


def pynvshmemx_int32_p_on_stream(dest: ctypes.c_void_p, value: np.intc, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_int32_p_on_stream(dest, value, pe, _stream_raw(stream))


def pynvshmemx_int64_p_on_stream(dest: ctypes.c_void_p, value: np.int_, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_int64_p_on_stream(dest, value, pe, _stream_raw(stream))


def pynvshmemx_uint8_p_on_stream(dest: ctypes.c_void_p, value: np.uint8, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_uint8_p_on_stream(dest, value, pe, _stream_raw(stream))


def pynvshmemx_uint16_p_on_stream(dest: ctypes.c_void_p, value: np.ushort, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_uint16_p_on_stream(dest, value, pe, _stream_raw(stream))


def pynvshmemx_uint32_p_on_stream(dest: ctypes.c_void_p, value: np.uintc, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_uint32_p_on_stream(dest, value, pe, _stream_raw(stream))


def pynvshmemx_uint64_p_on_stream(dest: ctypes.c_void_p, value: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_uint64_p_on_stream(dest, value, pe, _stream_raw(stream))


def pynvshmemx_size_p_on_stream(dest: ctypes.c_void_p, value: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_size_p_on_stream(dest, value, pe, _stream_raw(stream))


def pynvshmemx_ptrdiff_p_on_stream(dest: ctypes.c_void_p, value: np.int_, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_ptrdiff_p_on_stream(dest, value, pe, _stream_raw(stream))


def pynvshmemx_float_put_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_float_put_on_stream(dest, source, nelems, pe, _stream_raw(stream))


def pynvshmemx_double_put_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_double_put_on_stream(dest, source, nelems, pe, _stream_raw(stream))


def pynvshmemx_char_put_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_char_put_on_stream(dest, source, nelems, pe, _stream_raw(stream))


def pynvshmemx_short_put_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_short_put_on_stream(dest, source, nelems, pe, _stream_raw(stream))


def pynvshmemx_schar_put_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_schar_put_on_stream(dest, source, nelems, pe, _stream_raw(stream))


def pynvshmemx_int_put_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_int_put_on_stream(dest, source, nelems, pe, _stream_raw(stream))


def pynvshmemx_long_put_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_long_put_on_stream(dest, source, nelems, pe, _stream_raw(stream))


def pynvshmemx_longlong_put_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_longlong_put_on_stream(dest, source, nelems, pe, _stream_raw(stream))


def pynvshmemx_uchar_put_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_uchar_put_on_stream(dest, source, nelems, pe, _stream_raw(stream))


def pynvshmemx_ushort_put_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_ushort_put_on_stream(dest, source, nelems, pe, _stream_raw(stream))


def pynvshmemx_uint_put_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_uint_put_on_stream(dest, source, nelems, pe, _stream_raw(stream))


def pynvshmemx_ulong_put_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_ulong_put_on_stream(dest, source, nelems, pe, _stream_raw(stream))


def pynvshmemx_ulonglong_put_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_ulonglong_put_on_stream(dest, source, nelems, pe, _stream_raw(stream))


def pynvshmemx_int8_put_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_int8_put_on_stream(dest, source, nelems, pe, _stream_raw(stream))


def pynvshmemx_int16_put_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_int16_put_on_stream(dest, source, nelems, pe, _stream_raw(stream))


def pynvshmemx_int32_put_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_int32_put_on_stream(dest, source, nelems, pe, _stream_raw(stream))


def pynvshmemx_int64_put_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_int64_put_on_stream(dest, source, nelems, pe, _stream_raw(stream))


def pynvshmemx_uint8_put_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_uint8_put_on_stream(dest, source, nelems, pe, _stream_raw(stream))


def pynvshmemx_uint16_put_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_uint16_put_on_stream(dest, source, nelems, pe, _stream_raw(stream))


def pynvshmemx_uint32_put_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_uint32_put_on_stream(dest, source, nelems, pe, _stream_raw(stream))


def pynvshmemx_uint64_put_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_uint64_put_on_stream(dest, source, nelems, pe, _stream_raw(stream))


def pynvshmemx_size_put_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_size_put_on_stream(dest, source, nelems, pe, _stream_raw(stream))


def pynvshmemx_ptrdiff_put_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_ptrdiff_put_on_stream(dest, source, nelems, pe, _stream_raw(stream))


def pynvshmemx_put8_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_put8_on_stream(dest, source, nelems, pe, _stream_raw(stream))


def pynvshmemx_put16_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_put16_on_stream(dest, source, nelems, pe, _stream_raw(stream))


def pynvshmemx_put32_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_put32_on_stream(dest, source, nelems, pe, _stream_raw(stream))


def pynvshmemx_put64_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_put64_on_stream(dest, source, nelems, pe, _stream_raw(stream))


def pynvshmemx_put128_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_put128_on_stream(dest, source, nelems, pe, _stream_raw(stream))


def pynvshmemx_putmem_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, bytes: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_putmem_on_stream(dest, source, bytes, pe, _stream_raw(stream))


def pynvshmemx_float_put_signal_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, sig_addr: ctypes.c_void_p, signal: np.uint, sig_op: np.intc, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_float_put_signal_on_stream(dest, source, nelems, sig_addr, signal, sig_op, pe, _stream_raw(stream))


def pynvshmemx_double_put_signal_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, sig_addr: ctypes.c_void_p, signal: np.uint, sig_op: np.intc, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_double_put_signal_on_stream(dest, source, nelems, sig_addr, signal, sig_op, pe, _stream_raw(stream))


def pynvshmemx_char_put_signal_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, sig_addr: ctypes.c_void_p, signal: np.uint, sig_op: np.intc, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_char_put_signal_on_stream(dest, source, nelems, sig_addr, signal, sig_op, pe, _stream_raw(stream))


def pynvshmemx_short_put_signal_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, sig_addr: ctypes.c_void_p, signal: np.uint, sig_op: np.intc, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_short_put_signal_on_stream(dest, source, nelems, sig_addr, signal, sig_op, pe, _stream_raw(stream))


def pynvshmemx_schar_put_signal_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, sig_addr: ctypes.c_void_p, signal: np.uint, sig_op: np.intc, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_schar_put_signal_on_stream(dest, source, nelems, sig_addr, signal, sig_op, pe, _stream_raw(stream))


def pynvshmemx_int_put_signal_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, sig_addr: ctypes.c_void_p, signal: np.uint, sig_op: np.intc, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_int_put_signal_on_stream(dest, source, nelems, sig_addr, signal, sig_op, pe, _stream_raw(stream))


def pynvshmemx_long_put_signal_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, sig_addr: ctypes.c_void_p, signal: np.uint, sig_op: np.intc, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_long_put_signal_on_stream(dest, source, nelems, sig_addr, signal, sig_op, pe, _stream_raw(stream))


def pynvshmemx_longlong_put_signal_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, sig_addr: ctypes.c_void_p, signal: np.uint, sig_op: np.intc, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_longlong_put_signal_on_stream(dest, source, nelems, sig_addr, signal, sig_op, pe, _stream_raw(stream))


def pynvshmemx_uchar_put_signal_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, sig_addr: ctypes.c_void_p, signal: np.uint, sig_op: np.intc, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_uchar_put_signal_on_stream(dest, source, nelems, sig_addr, signal, sig_op, pe, _stream_raw(stream))


def pynvshmemx_ushort_put_signal_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, sig_addr: ctypes.c_void_p, signal: np.uint, sig_op: np.intc, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_ushort_put_signal_on_stream(dest, source, nelems, sig_addr, signal, sig_op, pe, _stream_raw(stream))


def pynvshmemx_uint_put_signal_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, sig_addr: ctypes.c_void_p, signal: np.uint, sig_op: np.intc, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_uint_put_signal_on_stream(dest, source, nelems, sig_addr, signal, sig_op, pe, _stream_raw(stream))


def pynvshmemx_ulong_put_signal_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, sig_addr: ctypes.c_void_p, signal: np.uint, sig_op: np.intc, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_ulong_put_signal_on_stream(dest, source, nelems, sig_addr, signal, sig_op, pe, _stream_raw(stream))


def pynvshmemx_ulonglong_put_signal_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, sig_addr: ctypes.c_void_p, signal: np.uint, sig_op: np.intc, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_ulonglong_put_signal_on_stream(dest, source, nelems, sig_addr, signal, sig_op, pe, _stream_raw(stream))


def pynvshmemx_int8_put_signal_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, sig_addr: ctypes.c_void_p, signal: np.uint, sig_op: np.intc, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_int8_put_signal_on_stream(dest, source, nelems, sig_addr, signal, sig_op, pe, _stream_raw(stream))


def pynvshmemx_int16_put_signal_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, sig_addr: ctypes.c_void_p, signal: np.uint, sig_op: np.intc, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_int16_put_signal_on_stream(dest, source, nelems, sig_addr, signal, sig_op, pe, _stream_raw(stream))


def pynvshmemx_int32_put_signal_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, sig_addr: ctypes.c_void_p, signal: np.uint, sig_op: np.intc, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_int32_put_signal_on_stream(dest, source, nelems, sig_addr, signal, sig_op, pe, _stream_raw(stream))


def pynvshmemx_int64_put_signal_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, sig_addr: ctypes.c_void_p, signal: np.uint, sig_op: np.intc, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_int64_put_signal_on_stream(dest, source, nelems, sig_addr, signal, sig_op, pe, _stream_raw(stream))


def pynvshmemx_uint8_put_signal_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, sig_addr: ctypes.c_void_p, signal: np.uint, sig_op: np.intc, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_uint8_put_signal_on_stream(dest, source, nelems, sig_addr, signal, sig_op, pe, _stream_raw(stream))


def pynvshmemx_uint16_put_signal_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, sig_addr: ctypes.c_void_p, signal: np.uint, sig_op: np.intc, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_uint16_put_signal_on_stream(dest, source, nelems, sig_addr, signal, sig_op, pe, _stream_raw(stream))


def pynvshmemx_uint32_put_signal_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, sig_addr: ctypes.c_void_p, signal: np.uint, sig_op: np.intc, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_uint32_put_signal_on_stream(dest, source, nelems, sig_addr, signal, sig_op, pe, _stream_raw(stream))


def pynvshmemx_uint64_put_signal_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, sig_addr: ctypes.c_void_p, signal: np.uint, sig_op: np.intc, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_uint64_put_signal_on_stream(dest, source, nelems, sig_addr, signal, sig_op, pe, _stream_raw(stream))


def pynvshmemx_size_put_signal_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, sig_addr: ctypes.c_void_p, signal: np.uint, sig_op: np.intc, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_size_put_signal_on_stream(dest, source, nelems, sig_addr, signal, sig_op, pe, _stream_raw(stream))


def pynvshmemx_ptrdiff_put_signal_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, sig_addr: ctypes.c_void_p, signal: np.uint, sig_op: np.intc, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_ptrdiff_put_signal_on_stream(dest, source, nelems, sig_addr, signal, sig_op, pe, _stream_raw(stream))


def pynvshmemx_put8_signal_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, sig_addr: ctypes.c_void_p, signal: np.uint, sig_op: np.intc, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_put8_signal_on_stream(dest, source, nelems, sig_addr, signal, sig_op, pe, _stream_raw(stream))


def pynvshmemx_put16_signal_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, sig_addr: ctypes.c_void_p, signal: np.uint, sig_op: np.intc, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_put16_signal_on_stream(dest, source, nelems, sig_addr, signal, sig_op, pe, _stream_raw(stream))


def pynvshmemx_put32_signal_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, sig_addr: ctypes.c_void_p, signal: np.uint, sig_op: np.intc, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_put32_signal_on_stream(dest, source, nelems, sig_addr, signal, sig_op, pe, _stream_raw(stream))


def pynvshmemx_put64_signal_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, sig_addr: ctypes.c_void_p, signal: np.uint, sig_op: np.intc, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_put64_signal_on_stream(dest, source, nelems, sig_addr, signal, sig_op, pe, _stream_raw(stream))


def pynvshmemx_put128_signal_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, sig_addr: ctypes.c_void_p, signal: np.uint, sig_op: np.intc, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_put128_signal_on_stream(dest, source, nelems, sig_addr, signal, sig_op, pe, _stream_raw(stream))


def pynvshmemx_putmem_signal_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, bytes: np.uint, sig_addr: ctypes.c_void_p, signal: np.uint, sig_op: np.intc, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_putmem_signal_on_stream(dest, source, bytes, sig_addr, signal, sig_op, pe, _stream_raw(stream))


def pynvshmemx_float_put_signal_nbi_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, sig_addr: ctypes.c_void_p, signal: np.uint, sig_op: np.intc, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_float_put_signal_nbi_on_stream(dest, source, nelems, sig_addr, signal, sig_op, pe, _stream_raw(stream))


def pynvshmemx_double_put_signal_nbi_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, sig_addr: ctypes.c_void_p, signal: np.uint, sig_op: np.intc, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_double_put_signal_nbi_on_stream(dest, source, nelems, sig_addr, signal, sig_op, pe, _stream_raw(stream))


def pynvshmemx_char_put_signal_nbi_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, sig_addr: ctypes.c_void_p, signal: np.uint, sig_op: np.intc, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_char_put_signal_nbi_on_stream(dest, source, nelems, sig_addr, signal, sig_op, pe, _stream_raw(stream))


def pynvshmemx_short_put_signal_nbi_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, sig_addr: ctypes.c_void_p, signal: np.uint, sig_op: np.intc, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_short_put_signal_nbi_on_stream(dest, source, nelems, sig_addr, signal, sig_op, pe, _stream_raw(stream))


def pynvshmemx_schar_put_signal_nbi_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, sig_addr: ctypes.c_void_p, signal: np.uint, sig_op: np.intc, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_schar_put_signal_nbi_on_stream(dest, source, nelems, sig_addr, signal, sig_op, pe, _stream_raw(stream))


def pynvshmemx_int_put_signal_nbi_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, sig_addr: ctypes.c_void_p, signal: np.uint, sig_op: np.intc, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_int_put_signal_nbi_on_stream(dest, source, nelems, sig_addr, signal, sig_op, pe, _stream_raw(stream))


def pynvshmemx_long_put_signal_nbi_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, sig_addr: ctypes.c_void_p, signal: np.uint, sig_op: np.intc, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_long_put_signal_nbi_on_stream(dest, source, nelems, sig_addr, signal, sig_op, pe, _stream_raw(stream))


def pynvshmemx_longlong_put_signal_nbi_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, sig_addr: ctypes.c_void_p, signal: np.uint, sig_op: np.intc, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_longlong_put_signal_nbi_on_stream(dest, source, nelems, sig_addr, signal, sig_op, pe, _stream_raw(stream))


def pynvshmemx_uchar_put_signal_nbi_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, sig_addr: ctypes.c_void_p, signal: np.uint, sig_op: np.intc, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_uchar_put_signal_nbi_on_stream(dest, source, nelems, sig_addr, signal, sig_op, pe, _stream_raw(stream))


def pynvshmemx_ushort_put_signal_nbi_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, sig_addr: ctypes.c_void_p, signal: np.uint, sig_op: np.intc, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_ushort_put_signal_nbi_on_stream(dest, source, nelems, sig_addr, signal, sig_op, pe, _stream_raw(stream))


def pynvshmemx_uint_put_signal_nbi_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, sig_addr: ctypes.c_void_p, signal: np.uint, sig_op: np.intc, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_uint_put_signal_nbi_on_stream(dest, source, nelems, sig_addr, signal, sig_op, pe, _stream_raw(stream))


def pynvshmemx_ulong_put_signal_nbi_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, sig_addr: ctypes.c_void_p, signal: np.uint, sig_op: np.intc, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_ulong_put_signal_nbi_on_stream(dest, source, nelems, sig_addr, signal, sig_op, pe, _stream_raw(stream))


def pynvshmemx_ulonglong_put_signal_nbi_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, sig_addr: ctypes.c_void_p, signal: np.uint, sig_op: np.intc, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_ulonglong_put_signal_nbi_on_stream(dest, source, nelems, sig_addr, signal, sig_op, pe, _stream_raw(stream))


def pynvshmemx_int8_put_signal_nbi_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, sig_addr: ctypes.c_void_p, signal: np.uint, sig_op: np.intc, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_int8_put_signal_nbi_on_stream(dest, source, nelems, sig_addr, signal, sig_op, pe, _stream_raw(stream))


def pynvshmemx_int16_put_signal_nbi_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, sig_addr: ctypes.c_void_p, signal: np.uint, sig_op: np.intc, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_int16_put_signal_nbi_on_stream(dest, source, nelems, sig_addr, signal, sig_op, pe, _stream_raw(stream))


def pynvshmemx_int32_put_signal_nbi_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, sig_addr: ctypes.c_void_p, signal: np.uint, sig_op: np.intc, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_int32_put_signal_nbi_on_stream(dest, source, nelems, sig_addr, signal, sig_op, pe, _stream_raw(stream))


def pynvshmemx_int64_put_signal_nbi_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, sig_addr: ctypes.c_void_p, signal: np.uint, sig_op: np.intc, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_int64_put_signal_nbi_on_stream(dest, source, nelems, sig_addr, signal, sig_op, pe, _stream_raw(stream))


def pynvshmemx_uint8_put_signal_nbi_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, sig_addr: ctypes.c_void_p, signal: np.uint, sig_op: np.intc, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_uint8_put_signal_nbi_on_stream(dest, source, nelems, sig_addr, signal, sig_op, pe, _stream_raw(stream))


def pynvshmemx_uint16_put_signal_nbi_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, sig_addr: ctypes.c_void_p, signal: np.uint, sig_op: np.intc, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_uint16_put_signal_nbi_on_stream(dest, source, nelems, sig_addr, signal, sig_op, pe, _stream_raw(stream))


def pynvshmemx_uint32_put_signal_nbi_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, sig_addr: ctypes.c_void_p, signal: np.uint, sig_op: np.intc, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_uint32_put_signal_nbi_on_stream(dest, source, nelems, sig_addr, signal, sig_op, pe, _stream_raw(stream))


def pynvshmemx_uint64_put_signal_nbi_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, sig_addr: ctypes.c_void_p, signal: np.uint, sig_op: np.intc, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_uint64_put_signal_nbi_on_stream(dest, source, nelems, sig_addr, signal, sig_op, pe, _stream_raw(stream))


def pynvshmemx_size_put_signal_nbi_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, sig_addr: ctypes.c_void_p, signal: np.uint, sig_op: np.intc, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_size_put_signal_nbi_on_stream(dest, source, nelems, sig_addr, signal, sig_op, pe, _stream_raw(stream))


def pynvshmemx_ptrdiff_put_signal_nbi_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, sig_addr: ctypes.c_void_p, signal: np.uint, sig_op: np.intc, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_ptrdiff_put_signal_nbi_on_stream(dest, source, nelems, sig_addr, signal, sig_op, pe, _stream_raw(stream))


def pynvshmemx_put8_signal_nbi_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, sig_addr: ctypes.c_void_p, signal: np.uint, sig_op: np.intc, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_put8_signal_nbi_on_stream(dest, source, nelems, sig_addr, signal, sig_op, pe, _stream_raw(stream))


def pynvshmemx_put16_signal_nbi_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, sig_addr: ctypes.c_void_p, signal: np.uint, sig_op: np.intc, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_put16_signal_nbi_on_stream(dest, source, nelems, sig_addr, signal, sig_op, pe, _stream_raw(stream))


def pynvshmemx_put32_signal_nbi_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, sig_addr: ctypes.c_void_p, signal: np.uint, sig_op: np.intc, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_put32_signal_nbi_on_stream(dest, source, nelems, sig_addr, signal, sig_op, pe, _stream_raw(stream))


def pynvshmemx_put64_signal_nbi_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, sig_addr: ctypes.c_void_p, signal: np.uint, sig_op: np.intc, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_put64_signal_nbi_on_stream(dest, source, nelems, sig_addr, signal, sig_op, pe, _stream_raw(stream))


def pynvshmemx_put128_signal_nbi_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, sig_addr: ctypes.c_void_p, signal: np.uint, sig_op: np.intc, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_put128_signal_nbi_on_stream(dest, source, nelems, sig_addr, signal, sig_op, pe, _stream_raw(stream))


def pynvshmemx_putmem_signal_nbi_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, bytes: np.uint, sig_addr: ctypes.c_void_p, signal: np.uint, sig_op: np.intc, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_putmem_signal_nbi_on_stream(dest, source, bytes, sig_addr, signal, sig_op, pe, _stream_raw(stream))


def pynvshmemx_float_iput_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, dst: np.int_, sst: np.int_, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_float_iput_on_stream(dest, source, dst, sst, nelems, pe, _stream_raw(stream))


def pynvshmemx_double_iput_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, dst: np.int_, sst: np.int_, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_double_iput_on_stream(dest, source, dst, sst, nelems, pe, _stream_raw(stream))


def pynvshmemx_char_iput_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, dst: np.int_, sst: np.int_, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_char_iput_on_stream(dest, source, dst, sst, nelems, pe, _stream_raw(stream))


def pynvshmemx_short_iput_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, dst: np.int_, sst: np.int_, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_short_iput_on_stream(dest, source, dst, sst, nelems, pe, _stream_raw(stream))


def pynvshmemx_schar_iput_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, dst: np.int_, sst: np.int_, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_schar_iput_on_stream(dest, source, dst, sst, nelems, pe, _stream_raw(stream))


def pynvshmemx_int_iput_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, dst: np.int_, sst: np.int_, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_int_iput_on_stream(dest, source, dst, sst, nelems, pe, _stream_raw(stream))


def pynvshmemx_long_iput_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, dst: np.int_, sst: np.int_, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_long_iput_on_stream(dest, source, dst, sst, nelems, pe, _stream_raw(stream))


def pynvshmemx_longlong_iput_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, dst: np.int_, sst: np.int_, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_longlong_iput_on_stream(dest, source, dst, sst, nelems, pe, _stream_raw(stream))


def pynvshmemx_uchar_iput_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, dst: np.int_, sst: np.int_, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_uchar_iput_on_stream(dest, source, dst, sst, nelems, pe, _stream_raw(stream))


def pynvshmemx_ushort_iput_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, dst: np.int_, sst: np.int_, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_ushort_iput_on_stream(dest, source, dst, sst, nelems, pe, _stream_raw(stream))


def pynvshmemx_uint_iput_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, dst: np.int_, sst: np.int_, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_uint_iput_on_stream(dest, source, dst, sst, nelems, pe, _stream_raw(stream))


def pynvshmemx_ulong_iput_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, dst: np.int_, sst: np.int_, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_ulong_iput_on_stream(dest, source, dst, sst, nelems, pe, _stream_raw(stream))


def pynvshmemx_ulonglong_iput_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, dst: np.int_, sst: np.int_, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_ulonglong_iput_on_stream(dest, source, dst, sst, nelems, pe, _stream_raw(stream))


def pynvshmemx_int8_iput_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, dst: np.int_, sst: np.int_, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_int8_iput_on_stream(dest, source, dst, sst, nelems, pe, _stream_raw(stream))


def pynvshmemx_int16_iput_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, dst: np.int_, sst: np.int_, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_int16_iput_on_stream(dest, source, dst, sst, nelems, pe, _stream_raw(stream))


def pynvshmemx_int32_iput_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, dst: np.int_, sst: np.int_, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_int32_iput_on_stream(dest, source, dst, sst, nelems, pe, _stream_raw(stream))


def pynvshmemx_int64_iput_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, dst: np.int_, sst: np.int_, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_int64_iput_on_stream(dest, source, dst, sst, nelems, pe, _stream_raw(stream))


def pynvshmemx_uint8_iput_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, dst: np.int_, sst: np.int_, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_uint8_iput_on_stream(dest, source, dst, sst, nelems, pe, _stream_raw(stream))


def pynvshmemx_uint16_iput_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, dst: np.int_, sst: np.int_, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_uint16_iput_on_stream(dest, source, dst, sst, nelems, pe, _stream_raw(stream))


def pynvshmemx_uint32_iput_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, dst: np.int_, sst: np.int_, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_uint32_iput_on_stream(dest, source, dst, sst, nelems, pe, _stream_raw(stream))


def pynvshmemx_uint64_iput_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, dst: np.int_, sst: np.int_, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_uint64_iput_on_stream(dest, source, dst, sst, nelems, pe, _stream_raw(stream))


def pynvshmemx_size_iput_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, dst: np.int_, sst: np.int_, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_size_iput_on_stream(dest, source, dst, sst, nelems, pe, _stream_raw(stream))


def pynvshmemx_ptrdiff_iput_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, dst: np.int_, sst: np.int_, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_ptrdiff_iput_on_stream(dest, source, dst, sst, nelems, pe, _stream_raw(stream))


def pynvshmemx_iput8_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, dst: np.int_, sst: np.int_, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_iput8_on_stream(dest, source, dst, sst, nelems, pe, _stream_raw(stream))


def pynvshmemx_iput16_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, dst: np.int_, sst: np.int_, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_iput16_on_stream(dest, source, dst, sst, nelems, pe, _stream_raw(stream))


def pynvshmemx_iput32_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, dst: np.int_, sst: np.int_, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_iput32_on_stream(dest, source, dst, sst, nelems, pe, _stream_raw(stream))


def pynvshmemx_iput64_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, dst: np.int_, sst: np.int_, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_iput64_on_stream(dest, source, dst, sst, nelems, pe, _stream_raw(stream))


def pynvshmemx_iput128_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, dst: np.int_, sst: np.int_, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_iput128_on_stream(dest, source, dst, sst, nelems, pe, _stream_raw(stream))


def pynvshmemx_float_put_nbi_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_float_put_nbi_on_stream(dest, source, nelems, pe, _stream_raw(stream))


def pynvshmemx_double_put_nbi_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_double_put_nbi_on_stream(dest, source, nelems, pe, _stream_raw(stream))


def pynvshmemx_char_put_nbi_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_char_put_nbi_on_stream(dest, source, nelems, pe, _stream_raw(stream))


def pynvshmemx_short_put_nbi_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_short_put_nbi_on_stream(dest, source, nelems, pe, _stream_raw(stream))


def pynvshmemx_schar_put_nbi_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_schar_put_nbi_on_stream(dest, source, nelems, pe, _stream_raw(stream))


def pynvshmemx_int_put_nbi_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_int_put_nbi_on_stream(dest, source, nelems, pe, _stream_raw(stream))


def pynvshmemx_long_put_nbi_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_long_put_nbi_on_stream(dest, source, nelems, pe, _stream_raw(stream))


def pynvshmemx_longlong_put_nbi_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_longlong_put_nbi_on_stream(dest, source, nelems, pe, _stream_raw(stream))


def pynvshmemx_uchar_put_nbi_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_uchar_put_nbi_on_stream(dest, source, nelems, pe, _stream_raw(stream))


def pynvshmemx_ushort_put_nbi_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_ushort_put_nbi_on_stream(dest, source, nelems, pe, _stream_raw(stream))


def pynvshmemx_uint_put_nbi_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_uint_put_nbi_on_stream(dest, source, nelems, pe, _stream_raw(stream))


def pynvshmemx_ulong_put_nbi_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_ulong_put_nbi_on_stream(dest, source, nelems, pe, _stream_raw(stream))


def pynvshmemx_ulonglong_put_nbi_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_ulonglong_put_nbi_on_stream(dest, source, nelems, pe, _stream_raw(stream))


def pynvshmemx_int8_put_nbi_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_int8_put_nbi_on_stream(dest, source, nelems, pe, _stream_raw(stream))


def pynvshmemx_int16_put_nbi_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_int16_put_nbi_on_stream(dest, source, nelems, pe, _stream_raw(stream))


def pynvshmemx_int32_put_nbi_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_int32_put_nbi_on_stream(dest, source, nelems, pe, _stream_raw(stream))


def pynvshmemx_int64_put_nbi_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_int64_put_nbi_on_stream(dest, source, nelems, pe, _stream_raw(stream))


def pynvshmemx_uint8_put_nbi_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_uint8_put_nbi_on_stream(dest, source, nelems, pe, _stream_raw(stream))


def pynvshmemx_uint16_put_nbi_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_uint16_put_nbi_on_stream(dest, source, nelems, pe, _stream_raw(stream))


def pynvshmemx_uint32_put_nbi_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_uint32_put_nbi_on_stream(dest, source, nelems, pe, _stream_raw(stream))


def pynvshmemx_uint64_put_nbi_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_uint64_put_nbi_on_stream(dest, source, nelems, pe, _stream_raw(stream))


def pynvshmemx_size_put_nbi_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_size_put_nbi_on_stream(dest, source, nelems, pe, _stream_raw(stream))


def pynvshmemx_ptrdiff_put_nbi_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_ptrdiff_put_nbi_on_stream(dest, source, nelems, pe, _stream_raw(stream))


def pynvshmemx_put8_nbi_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_put8_nbi_on_stream(dest, source, nelems, pe, _stream_raw(stream))


def pynvshmemx_put16_nbi_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_put16_nbi_on_stream(dest, source, nelems, pe, _stream_raw(stream))


def pynvshmemx_put32_nbi_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_put32_nbi_on_stream(dest, source, nelems, pe, _stream_raw(stream))


def pynvshmemx_put64_nbi_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_put64_nbi_on_stream(dest, source, nelems, pe, _stream_raw(stream))


def pynvshmemx_put128_nbi_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_put128_nbi_on_stream(dest, source, nelems, pe, _stream_raw(stream))


def pynvshmemx_putmem_nbi_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, bytes: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_putmem_nbi_on_stream(dest, source, bytes, pe, _stream_raw(stream))


def pynvshmemx_float_g_on_stream(src: ctypes.c_void_p, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> np.float32:
    return flux.pynvshmem_mod._pynvshmemx_float_g_on_stream(src, pe, _stream_raw(stream))


def pynvshmemx_double_g_on_stream(src: ctypes.c_void_p, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> np.float64:
    return flux.pynvshmem_mod._pynvshmemx_double_g_on_stream(src, pe, _stream_raw(stream))


def pynvshmemx_char_g_on_stream(src: ctypes.c_void_p, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> np.char:
    return flux.pynvshmem_mod._pynvshmemx_char_g_on_stream(src, pe, _stream_raw(stream))


def pynvshmemx_short_g_on_stream(src: ctypes.c_void_p, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> np.short:
    return flux.pynvshmem_mod._pynvshmemx_short_g_on_stream(src, pe, _stream_raw(stream))


def pynvshmemx_schar_g_on_stream(src: ctypes.c_void_p, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> np.char:
    return flux.pynvshmem_mod._pynvshmemx_schar_g_on_stream(src, pe, _stream_raw(stream))


def pynvshmemx_int_g_on_stream(src: ctypes.c_void_p, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> np.intc:
    return flux.pynvshmem_mod._pynvshmemx_int_g_on_stream(src, pe, _stream_raw(stream))


def pynvshmemx_long_g_on_stream(src: ctypes.c_void_p, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> np.int_:
    return flux.pynvshmem_mod._pynvshmemx_long_g_on_stream(src, pe, _stream_raw(stream))


def pynvshmemx_longlong_g_on_stream(src: ctypes.c_void_p, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> np.longlong:
    return flux.pynvshmem_mod._pynvshmemx_longlong_g_on_stream(src, pe, _stream_raw(stream))


def pynvshmemx_uchar_g_on_stream(src: ctypes.c_void_p, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> np.uint8:
    return flux.pynvshmem_mod._pynvshmemx_uchar_g_on_stream(src, pe, _stream_raw(stream))


def pynvshmemx_ushort_g_on_stream(src: ctypes.c_void_p, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> np.ushort:
    return flux.pynvshmem_mod._pynvshmemx_ushort_g_on_stream(src, pe, _stream_raw(stream))


def pynvshmemx_uint_g_on_stream(src: ctypes.c_void_p, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> np.uintc:
    return flux.pynvshmem_mod._pynvshmemx_uint_g_on_stream(src, pe, _stream_raw(stream))


def pynvshmemx_ulong_g_on_stream(src: ctypes.c_void_p, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> np.uint:
    return flux.pynvshmem_mod._pynvshmemx_ulong_g_on_stream(src, pe, _stream_raw(stream))


def pynvshmemx_ulonglong_g_on_stream(src: ctypes.c_void_p, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> np.ulonglong:
    return flux.pynvshmem_mod._pynvshmemx_ulonglong_g_on_stream(src, pe, _stream_raw(stream))


def pynvshmemx_int8_g_on_stream(src: ctypes.c_void_p, pe: np.intc, stream: CUDA_STREAM_TYPE) :
    return flux.pynvshmem_mod._pynvshmemx_int8_g_on_stream(src, pe, _stream_raw(stream))


def pynvshmemx_int16_g_on_stream(src: ctypes.c_void_p, pe: np.intc, stream: CUDA_STREAM_TYPE) :
    return flux.pynvshmem_mod._pynvshmemx_int16_g_on_stream(src, pe, _stream_raw(stream))


def pynvshmemx_int32_g_on_stream(src: ctypes.c_void_p, pe: np.intc, stream: CUDA_STREAM_TYPE) :
    return flux.pynvshmem_mod._pynvshmemx_int32_g_on_stream(src, pe, _stream_raw(stream))


def pynvshmemx_int64_g_on_stream(src: ctypes.c_void_p, pe: np.intc, stream: CUDA_STREAM_TYPE) :
    return flux.pynvshmem_mod._pynvshmemx_int64_g_on_stream(src, pe, _stream_raw(stream))


def pynvshmemx_uint8_g_on_stream(src: ctypes.c_void_p, pe: np.intc, stream: CUDA_STREAM_TYPE) :
    return flux.pynvshmem_mod._pynvshmemx_uint8_g_on_stream(src, pe, _stream_raw(stream))


def pynvshmemx_uint16_g_on_stream(src: ctypes.c_void_p, pe: np.intc, stream: CUDA_STREAM_TYPE) :
    return flux.pynvshmem_mod._pynvshmemx_uint16_g_on_stream(src, pe, _stream_raw(stream))


def pynvshmemx_uint32_g_on_stream(src: ctypes.c_void_p, pe: np.intc, stream: CUDA_STREAM_TYPE) :
    return flux.pynvshmem_mod._pynvshmemx_uint32_g_on_stream(src, pe, _stream_raw(stream))


def pynvshmemx_uint64_g_on_stream(src: ctypes.c_void_p, pe: np.intc, stream: CUDA_STREAM_TYPE) :
    return flux.pynvshmem_mod._pynvshmemx_uint64_g_on_stream(src, pe, _stream_raw(stream))


def pynvshmemx_size_g_on_stream(src: ctypes.c_void_p, pe: np.intc, stream: CUDA_STREAM_TYPE) :
    return flux.pynvshmem_mod._pynvshmemx_size_g_on_stream(src, pe, _stream_raw(stream))


def pynvshmemx_ptrdiff_g_on_stream(src: ctypes.c_void_p, pe: np.intc, stream: CUDA_STREAM_TYPE) :
    return flux.pynvshmem_mod._pynvshmemx_ptrdiff_g_on_stream(src, pe, _stream_raw(stream))


def pynvshmemx_float_get_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_float_get_on_stream(dest, source, nelems, pe, _stream_raw(stream))


def pynvshmemx_double_get_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_double_get_on_stream(dest, source, nelems, pe, _stream_raw(stream))


def pynvshmemx_char_get_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_char_get_on_stream(dest, source, nelems, pe, _stream_raw(stream))


def pynvshmemx_short_get_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_short_get_on_stream(dest, source, nelems, pe, _stream_raw(stream))


def pynvshmemx_schar_get_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_schar_get_on_stream(dest, source, nelems, pe, _stream_raw(stream))


def pynvshmemx_int_get_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_int_get_on_stream(dest, source, nelems, pe, _stream_raw(stream))


def pynvshmemx_long_get_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_long_get_on_stream(dest, source, nelems, pe, _stream_raw(stream))


def pynvshmemx_longlong_get_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_longlong_get_on_stream(dest, source, nelems, pe, _stream_raw(stream))


def pynvshmemx_uchar_get_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_uchar_get_on_stream(dest, source, nelems, pe, _stream_raw(stream))


def pynvshmemx_ushort_get_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_ushort_get_on_stream(dest, source, nelems, pe, _stream_raw(stream))


def pynvshmemx_uint_get_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_uint_get_on_stream(dest, source, nelems, pe, _stream_raw(stream))


def pynvshmemx_ulong_get_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_ulong_get_on_stream(dest, source, nelems, pe, _stream_raw(stream))


def pynvshmemx_ulonglong_get_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_ulonglong_get_on_stream(dest, source, nelems, pe, _stream_raw(stream))


def pynvshmemx_int8_get_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_int8_get_on_stream(dest, source, nelems, pe, _stream_raw(stream))


def pynvshmemx_int16_get_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_int16_get_on_stream(dest, source, nelems, pe, _stream_raw(stream))


def pynvshmemx_int32_get_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_int32_get_on_stream(dest, source, nelems, pe, _stream_raw(stream))


def pynvshmemx_int64_get_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_int64_get_on_stream(dest, source, nelems, pe, _stream_raw(stream))


def pynvshmemx_uint8_get_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_uint8_get_on_stream(dest, source, nelems, pe, _stream_raw(stream))


def pynvshmemx_uint16_get_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_uint16_get_on_stream(dest, source, nelems, pe, _stream_raw(stream))


def pynvshmemx_uint32_get_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_uint32_get_on_stream(dest, source, nelems, pe, _stream_raw(stream))


def pynvshmemx_uint64_get_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_uint64_get_on_stream(dest, source, nelems, pe, _stream_raw(stream))


def pynvshmemx_size_get_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_size_get_on_stream(dest, source, nelems, pe, _stream_raw(stream))


def pynvshmemx_ptrdiff_get_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_ptrdiff_get_on_stream(dest, source, nelems, pe, _stream_raw(stream))


def pynvshmemx_float_iget_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, dst: np.int_, sst: np.int_, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_float_iget_on_stream(dest, source, dst, sst, nelems, pe, _stream_raw(stream))


def pynvshmemx_double_iget_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, dst: np.int_, sst: np.int_, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_double_iget_on_stream(dest, source, dst, sst, nelems, pe, _stream_raw(stream))


def pynvshmemx_char_iget_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, dst: np.int_, sst: np.int_, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_char_iget_on_stream(dest, source, dst, sst, nelems, pe, _stream_raw(stream))


def pynvshmemx_short_iget_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, dst: np.int_, sst: np.int_, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_short_iget_on_stream(dest, source, dst, sst, nelems, pe, _stream_raw(stream))


def pynvshmemx_schar_iget_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, dst: np.int_, sst: np.int_, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_schar_iget_on_stream(dest, source, dst, sst, nelems, pe, _stream_raw(stream))


def pynvshmemx_int_iget_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, dst: np.int_, sst: np.int_, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_int_iget_on_stream(dest, source, dst, sst, nelems, pe, _stream_raw(stream))


def pynvshmemx_long_iget_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, dst: np.int_, sst: np.int_, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_long_iget_on_stream(dest, source, dst, sst, nelems, pe, _stream_raw(stream))


def pynvshmemx_longlong_iget_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, dst: np.int_, sst: np.int_, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_longlong_iget_on_stream(dest, source, dst, sst, nelems, pe, _stream_raw(stream))


def pynvshmemx_uchar_iget_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, dst: np.int_, sst: np.int_, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_uchar_iget_on_stream(dest, source, dst, sst, nelems, pe, _stream_raw(stream))


def pynvshmemx_ushort_iget_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, dst: np.int_, sst: np.int_, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_ushort_iget_on_stream(dest, source, dst, sst, nelems, pe, _stream_raw(stream))


def pynvshmemx_uint_iget_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, dst: np.int_, sst: np.int_, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_uint_iget_on_stream(dest, source, dst, sst, nelems, pe, _stream_raw(stream))


def pynvshmemx_ulong_iget_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, dst: np.int_, sst: np.int_, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_ulong_iget_on_stream(dest, source, dst, sst, nelems, pe, _stream_raw(stream))


def pynvshmemx_ulonglong_iget_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, dst: np.int_, sst: np.int_, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_ulonglong_iget_on_stream(dest, source, dst, sst, nelems, pe, _stream_raw(stream))


def pynvshmemx_int8_iget_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, dst: np.int_, sst: np.int_, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_int8_iget_on_stream(dest, source, dst, sst, nelems, pe, _stream_raw(stream))


def pynvshmemx_int16_iget_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, dst: np.int_, sst: np.int_, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_int16_iget_on_stream(dest, source, dst, sst, nelems, pe, _stream_raw(stream))


def pynvshmemx_int32_iget_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, dst: np.int_, sst: np.int_, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_int32_iget_on_stream(dest, source, dst, sst, nelems, pe, _stream_raw(stream))


def pynvshmemx_int64_iget_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, dst: np.int_, sst: np.int_, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_int64_iget_on_stream(dest, source, dst, sst, nelems, pe, _stream_raw(stream))


def pynvshmemx_uint8_iget_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, dst: np.int_, sst: np.int_, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_uint8_iget_on_stream(dest, source, dst, sst, nelems, pe, _stream_raw(stream))


def pynvshmemx_uint16_iget_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, dst: np.int_, sst: np.int_, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_uint16_iget_on_stream(dest, source, dst, sst, nelems, pe, _stream_raw(stream))


def pynvshmemx_uint32_iget_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, dst: np.int_, sst: np.int_, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_uint32_iget_on_stream(dest, source, dst, sst, nelems, pe, _stream_raw(stream))


def pynvshmemx_uint64_iget_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, dst: np.int_, sst: np.int_, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_uint64_iget_on_stream(dest, source, dst, sst, nelems, pe, _stream_raw(stream))


def pynvshmemx_size_iget_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, dst: np.int_, sst: np.int_, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_size_iget_on_stream(dest, source, dst, sst, nelems, pe, _stream_raw(stream))


def pynvshmemx_ptrdiff_iget_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, dst: np.int_, sst: np.int_, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_ptrdiff_iget_on_stream(dest, source, dst, sst, nelems, pe, _stream_raw(stream))


def pynvshmemx_get8_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_get8_on_stream(dest, source, nelems, pe, _stream_raw(stream))


def pynvshmemx_get16_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_get16_on_stream(dest, source, nelems, pe, _stream_raw(stream))


def pynvshmemx_get32_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_get32_on_stream(dest, source, nelems, pe, _stream_raw(stream))


def pynvshmemx_get64_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_get64_on_stream(dest, source, nelems, pe, _stream_raw(stream))


def pynvshmemx_get128_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_get128_on_stream(dest, source, nelems, pe, _stream_raw(stream))


def pynvshmemx_getmem_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, bytes: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_getmem_on_stream(dest, source, bytes, pe, _stream_raw(stream))


def pynvshmemx_iget8_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, dst: np.int_, sst: np.int_, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_iget8_on_stream(dest, source, dst, sst, nelems, pe, _stream_raw(stream))


def pynvshmemx_iget16_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, dst: np.int_, sst: np.int_, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_iget16_on_stream(dest, source, dst, sst, nelems, pe, _stream_raw(stream))


def pynvshmemx_iget32_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, dst: np.int_, sst: np.int_, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_iget32_on_stream(dest, source, dst, sst, nelems, pe, _stream_raw(stream))


def pynvshmemx_iget64_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, dst: np.int_, sst: np.int_, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_iget64_on_stream(dest, source, dst, sst, nelems, pe, _stream_raw(stream))


def pynvshmemx_iget128_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, dst: np.int_, sst: np.int_, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_iget128_on_stream(dest, source, dst, sst, nelems, pe, _stream_raw(stream))


def pynvshmemx_float_get_nbi_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_float_get_nbi_on_stream(dest, source, nelems, pe, _stream_raw(stream))


def pynvshmemx_double_get_nbi_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_double_get_nbi_on_stream(dest, source, nelems, pe, _stream_raw(stream))


def pynvshmemx_char_get_nbi_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_char_get_nbi_on_stream(dest, source, nelems, pe, _stream_raw(stream))


def pynvshmemx_short_get_nbi_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_short_get_nbi_on_stream(dest, source, nelems, pe, _stream_raw(stream))


def pynvshmemx_schar_get_nbi_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_schar_get_nbi_on_stream(dest, source, nelems, pe, _stream_raw(stream))


def pynvshmemx_int_get_nbi_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_int_get_nbi_on_stream(dest, source, nelems, pe, _stream_raw(stream))


def pynvshmemx_long_get_nbi_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_long_get_nbi_on_stream(dest, source, nelems, pe, _stream_raw(stream))


def pynvshmemx_longlong_get_nbi_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_longlong_get_nbi_on_stream(dest, source, nelems, pe, _stream_raw(stream))


def pynvshmemx_uchar_get_nbi_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_uchar_get_nbi_on_stream(dest, source, nelems, pe, _stream_raw(stream))


def pynvshmemx_ushort_get_nbi_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_ushort_get_nbi_on_stream(dest, source, nelems, pe, _stream_raw(stream))


def pynvshmemx_uint_get_nbi_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_uint_get_nbi_on_stream(dest, source, nelems, pe, _stream_raw(stream))


def pynvshmemx_ulong_get_nbi_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_ulong_get_nbi_on_stream(dest, source, nelems, pe, _stream_raw(stream))


def pynvshmemx_ulonglong_get_nbi_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_ulonglong_get_nbi_on_stream(dest, source, nelems, pe, _stream_raw(stream))


def pynvshmemx_int8_get_nbi_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_int8_get_nbi_on_stream(dest, source, nelems, pe, _stream_raw(stream))


def pynvshmemx_int16_get_nbi_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_int16_get_nbi_on_stream(dest, source, nelems, pe, _stream_raw(stream))


def pynvshmemx_int32_get_nbi_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_int32_get_nbi_on_stream(dest, source, nelems, pe, _stream_raw(stream))


def pynvshmemx_int64_get_nbi_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_int64_get_nbi_on_stream(dest, source, nelems, pe, _stream_raw(stream))


def pynvshmemx_uint8_get_nbi_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_uint8_get_nbi_on_stream(dest, source, nelems, pe, _stream_raw(stream))


def pynvshmemx_uint16_get_nbi_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_uint16_get_nbi_on_stream(dest, source, nelems, pe, _stream_raw(stream))


def pynvshmemx_uint32_get_nbi_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_uint32_get_nbi_on_stream(dest, source, nelems, pe, _stream_raw(stream))


def pynvshmemx_uint64_get_nbi_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_uint64_get_nbi_on_stream(dest, source, nelems, pe, _stream_raw(stream))


def pynvshmemx_size_get_nbi_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_size_get_nbi_on_stream(dest, source, nelems, pe, _stream_raw(stream))


def pynvshmemx_ptrdiff_get_nbi_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_ptrdiff_get_nbi_on_stream(dest, source, nelems, pe, _stream_raw(stream))


def pynvshmemx_get8_nbi_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_get8_nbi_on_stream(dest, source, nelems, pe, _stream_raw(stream))


def pynvshmemx_get16_nbi_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_get16_nbi_on_stream(dest, source, nelems, pe, _stream_raw(stream))


def pynvshmemx_get32_nbi_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_get32_nbi_on_stream(dest, source, nelems, pe, _stream_raw(stream))


def pynvshmemx_get64_nbi_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_get64_nbi_on_stream(dest, source, nelems, pe, _stream_raw(stream))


def pynvshmemx_get128_nbi_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, nelems: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_get128_nbi_on_stream(dest, source, nelems, pe, _stream_raw(stream))


def pynvshmemx_getmem_nbi_on_stream(dest: ctypes.c_void_p, source: ctypes.c_void_p, bytes: np.uint, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_getmem_nbi_on_stream(dest, source, bytes, pe, _stream_raw(stream))


def pynvshmemx_quiet_on_stream(stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_quiet_on_stream(_stream_raw(stream))


def pynvshmemx_signal_op_on_stream(sig_addr: ctypes.c_void_p, signal: np.uint, sig_op: np.intc, pe: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_signal_op_on_stream(sig_addr, signal, sig_op, pe, _stream_raw(stream))


def pynvshmemx_short_wait_until_on_stream(ivar: ctypes.c_void_p, cmp: np.intc, cmp_value: np.short, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_short_wait_until_on_stream(ivar, cmp, cmp_value, _stream_raw(stream))


def pynvshmemx_int_wait_until_on_stream(ivar: ctypes.c_void_p, cmp: np.intc, cmp_value: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_int_wait_until_on_stream(ivar, cmp, cmp_value, _stream_raw(stream))


def pynvshmemx_long_wait_until_on_stream(ivar: ctypes.c_void_p, cmp: np.intc, cmp_value: np.int_, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_long_wait_until_on_stream(ivar, cmp, cmp_value, _stream_raw(stream))


def pynvshmemx_longlong_wait_until_on_stream(ivar: ctypes.c_void_p, cmp: np.intc, cmp_value: np.longlong, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_longlong_wait_until_on_stream(ivar, cmp, cmp_value, _stream_raw(stream))


def pynvshmemx_ushort_wait_until_on_stream(ivar: ctypes.c_void_p, cmp: np.intc, cmp_value: np.ushort, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_ushort_wait_until_on_stream(ivar, cmp, cmp_value, _stream_raw(stream))


def pynvshmemx_uint_wait_until_on_stream(ivar: ctypes.c_void_p, cmp: np.intc, cmp_value: np.uintc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_uint_wait_until_on_stream(ivar, cmp, cmp_value, _stream_raw(stream))


def pynvshmemx_ulong_wait_until_on_stream(ivar: ctypes.c_void_p, cmp: np.intc, cmp_value: np.uint, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_ulong_wait_until_on_stream(ivar, cmp, cmp_value, _stream_raw(stream))


def pynvshmemx_ulonglong_wait_until_on_stream(ivar: ctypes.c_void_p, cmp: np.intc, cmp_value: np.ulonglong, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_ulonglong_wait_until_on_stream(ivar, cmp, cmp_value, _stream_raw(stream))


def pynvshmemx_int32_wait_until_on_stream(ivar: ctypes.c_void_p, cmp: np.intc, cmp_value: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_int32_wait_until_on_stream(ivar, cmp, cmp_value, _stream_raw(stream))


def pynvshmemx_int64_wait_until_on_stream(ivar: ctypes.c_void_p, cmp: np.intc, cmp_value: np.int_, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_int64_wait_until_on_stream(ivar, cmp, cmp_value, _stream_raw(stream))


def pynvshmemx_uint32_wait_until_on_stream(ivar: ctypes.c_void_p, cmp: np.intc, cmp_value: np.uintc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_uint32_wait_until_on_stream(ivar, cmp, cmp_value, _stream_raw(stream))


def pynvshmemx_uint64_wait_until_on_stream(ivar: ctypes.c_void_p, cmp: np.intc, cmp_value: np.uint, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_uint64_wait_until_on_stream(ivar, cmp, cmp_value, _stream_raw(stream))


def pynvshmemx_size_wait_until_on_stream(ivar: ctypes.c_void_p, cmp: np.intc, cmp_value: np.uint, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_size_wait_until_on_stream(ivar, cmp, cmp_value, _stream_raw(stream))


def pynvshmemx_ptrdiff_wait_until_on_stream(ivar: ctypes.c_void_p, cmp: np.intc, cmp_value: np.int_, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_ptrdiff_wait_until_on_stream(ivar, cmp, cmp_value, _stream_raw(stream))


def pynvshmemx_short_wait_until_all_on_stream(ivars: ctypes.c_void_p, nelems: np.uint, status: ctypes.c_void_p, cmp: np.intc, cmp_value: np.short, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_short_wait_until_all_on_stream(ivars, nelems, status, cmp, cmp_value, _stream_raw(stream))


def pynvshmemx_int_wait_until_all_on_stream(ivars: ctypes.c_void_p, nelems: np.uint, status: ctypes.c_void_p, cmp: np.intc, cmp_value: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_int_wait_until_all_on_stream(ivars, nelems, status, cmp, cmp_value, _stream_raw(stream))


def pynvshmemx_long_wait_until_all_on_stream(ivars: ctypes.c_void_p, nelems: np.uint, status: ctypes.c_void_p, cmp: np.intc, cmp_value: np.int_, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_long_wait_until_all_on_stream(ivars, nelems, status, cmp, cmp_value, _stream_raw(stream))


def pynvshmemx_longlong_wait_until_all_on_stream(ivars: ctypes.c_void_p, nelems: np.uint, status: ctypes.c_void_p, cmp: np.intc, cmp_value: np.longlong, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_longlong_wait_until_all_on_stream(ivars, nelems, status, cmp, cmp_value, _stream_raw(stream))


def pynvshmemx_ushort_wait_until_all_on_stream(ivars: ctypes.c_void_p, nelems: np.uint, status: ctypes.c_void_p, cmp: np.intc, cmp_value: np.ushort, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_ushort_wait_until_all_on_stream(ivars, nelems, status, cmp, cmp_value, _stream_raw(stream))


def pynvshmemx_uint_wait_until_all_on_stream(ivars: ctypes.c_void_p, nelems: np.uint, status: ctypes.c_void_p, cmp: np.intc, cmp_value: np.uintc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_uint_wait_until_all_on_stream(ivars, nelems, status, cmp, cmp_value, _stream_raw(stream))


def pynvshmemx_ulong_wait_until_all_on_stream(ivars: ctypes.c_void_p, nelems: np.uint, status: ctypes.c_void_p, cmp: np.intc, cmp_value: np.uint, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_ulong_wait_until_all_on_stream(ivars, nelems, status, cmp, cmp_value, _stream_raw(stream))


def pynvshmemx_ulonglong_wait_until_all_on_stream(ivars: ctypes.c_void_p, nelems: np.uint, status: ctypes.c_void_p, cmp: np.intc, cmp_value: np.ulonglong, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_ulonglong_wait_until_all_on_stream(ivars, nelems, status, cmp, cmp_value, _stream_raw(stream))


def pynvshmemx_int32_wait_until_all_on_stream(ivars: ctypes.c_void_p, nelems: np.uint, status: ctypes.c_void_p, cmp: np.intc, cmp_value: np.intc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_int32_wait_until_all_on_stream(ivars, nelems, status, cmp, cmp_value, _stream_raw(stream))


def pynvshmemx_int64_wait_until_all_on_stream(ivars: ctypes.c_void_p, nelems: np.uint, status: ctypes.c_void_p, cmp: np.intc, cmp_value: np.int_, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_int64_wait_until_all_on_stream(ivars, nelems, status, cmp, cmp_value, _stream_raw(stream))


def pynvshmemx_uint32_wait_until_all_on_stream(ivars: ctypes.c_void_p, nelems: np.uint, status: ctypes.c_void_p, cmp: np.intc, cmp_value: np.uintc, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_uint32_wait_until_all_on_stream(ivars, nelems, status, cmp, cmp_value, _stream_raw(stream))


def pynvshmemx_uint64_wait_until_all_on_stream(ivars: ctypes.c_void_p, nelems: np.uint, status: ctypes.c_void_p, cmp: np.intc, cmp_value: np.uint, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_uint64_wait_until_all_on_stream(ivars, nelems, status, cmp, cmp_value, _stream_raw(stream))


def pynvshmemx_size_wait_until_all_on_stream(ivars: ctypes.c_void_p, nelems: np.uint, status: ctypes.c_void_p, cmp: np.intc, cmp_value: np.uint, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_size_wait_until_all_on_stream(ivars, nelems, status, cmp, cmp_value, _stream_raw(stream))


def pynvshmemx_ptrdiff_wait_until_all_on_stream(ivars: ctypes.c_void_p, nelems: np.uint, status: ctypes.c_void_p, cmp: np.intc, cmp_value: np.int_, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_ptrdiff_wait_until_all_on_stream(ivars, nelems, status, cmp, cmp_value, _stream_raw(stream))


def pynvshmemx_short_wait_until_all_vector_on_stream(ivars: ctypes.c_void_p, nelems: np.uint, status: ctypes.c_void_p, cmp: np.intc, cmp_value: ctypes.c_void_p, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_short_wait_until_all_vector_on_stream(ivars, nelems, status, cmp, cmp_value, _stream_raw(stream))


def pynvshmemx_int_wait_until_all_vector_on_stream(ivars: ctypes.c_void_p, nelems: np.uint, status: ctypes.c_void_p, cmp: np.intc, cmp_value: ctypes.c_void_p, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_int_wait_until_all_vector_on_stream(ivars, nelems, status, cmp, cmp_value, _stream_raw(stream))


def pynvshmemx_long_wait_until_all_vector_on_stream(ivars: ctypes.c_void_p, nelems: np.uint, status: ctypes.c_void_p, cmp: np.intc, cmp_value: ctypes.c_void_p, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_long_wait_until_all_vector_on_stream(ivars, nelems, status, cmp, cmp_value, _stream_raw(stream))


def pynvshmemx_longlong_wait_until_all_vector_on_stream(ivars: ctypes.c_void_p, nelems: np.uint, status: ctypes.c_void_p, cmp: np.intc, cmp_value: ctypes.c_void_p, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_longlong_wait_until_all_vector_on_stream(ivars, nelems, status, cmp, cmp_value, _stream_raw(stream))


def pynvshmemx_ushort_wait_until_all_vector_on_stream(ivars: ctypes.c_void_p, nelems: np.uint, status: ctypes.c_void_p, cmp: np.intc, cmp_value: ctypes.c_void_p, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_ushort_wait_until_all_vector_on_stream(ivars, nelems, status, cmp, cmp_value, _stream_raw(stream))


def pynvshmemx_uint_wait_until_all_vector_on_stream(ivars: ctypes.c_void_p, nelems: np.uint, status: ctypes.c_void_p, cmp: np.intc, cmp_value: ctypes.c_void_p, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_uint_wait_until_all_vector_on_stream(ivars, nelems, status, cmp, cmp_value, _stream_raw(stream))


def pynvshmemx_ulong_wait_until_all_vector_on_stream(ivars: ctypes.c_void_p, nelems: np.uint, status: ctypes.c_void_p, cmp: np.intc, cmp_value: ctypes.c_void_p, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_ulong_wait_until_all_vector_on_stream(ivars, nelems, status, cmp, cmp_value, _stream_raw(stream))


def pynvshmemx_ulonglong_wait_until_all_vector_on_stream(ivars: ctypes.c_void_p, nelems: np.uint, status: ctypes.c_void_p, cmp: np.intc, cmp_value: ctypes.c_void_p, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_ulonglong_wait_until_all_vector_on_stream(ivars, nelems, status, cmp, cmp_value, _stream_raw(stream))


def pynvshmemx_int32_wait_until_all_vector_on_stream(ivars: ctypes.c_void_p, nelems: np.uint, status: ctypes.c_void_p, cmp: np.intc, cmp_value: ctypes.c_void_p, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_int32_wait_until_all_vector_on_stream(ivars, nelems, status, cmp, cmp_value, _stream_raw(stream))


def pynvshmemx_int64_wait_until_all_vector_on_stream(ivars: ctypes.c_void_p, nelems: np.uint, status: ctypes.c_void_p, cmp: np.intc, cmp_value: ctypes.c_void_p, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_int64_wait_until_all_vector_on_stream(ivars, nelems, status, cmp, cmp_value, _stream_raw(stream))


def pynvshmemx_uint32_wait_until_all_vector_on_stream(ivars: ctypes.c_void_p, nelems: np.uint, status: ctypes.c_void_p, cmp: np.intc, cmp_value: ctypes.c_void_p, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_uint32_wait_until_all_vector_on_stream(ivars, nelems, status, cmp, cmp_value, _stream_raw(stream))


def pynvshmemx_uint64_wait_until_all_vector_on_stream(ivars: ctypes.c_void_p, nelems: np.uint, status: ctypes.c_void_p, cmp: np.intc, cmp_value: ctypes.c_void_p, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_uint64_wait_until_all_vector_on_stream(ivars, nelems, status, cmp, cmp_value, _stream_raw(stream))


def pynvshmemx_size_wait_until_all_vector_on_stream(ivars: ctypes.c_void_p, nelems: np.uint, status: ctypes.c_void_p, cmp: np.intc, cmp_value: ctypes.c_void_p, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_size_wait_until_all_vector_on_stream(ivars, nelems, status, cmp, cmp_value, _stream_raw(stream))


def pynvshmemx_ptrdiff_wait_until_all_vector_on_stream(ivars: ctypes.c_void_p, nelems: np.uint, status: ctypes.c_void_p, cmp: np.intc, cmp_value: ctypes.c_void_p, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_ptrdiff_wait_until_all_vector_on_stream(ivars, nelems, status, cmp, cmp_value, _stream_raw(stream))


def pynvshmemx_signal_wait_until_on_stream(sig_addr: ctypes.c_void_p, cmp: np.intc, cmp_value: np.uint, stream: CUDA_STREAM_TYPE)  -> None:
    return flux.pynvshmem_mod._pynvshmemx_signal_wait_until_on_stream(sig_addr, cmp, cmp_value, _stream_raw(stream))
