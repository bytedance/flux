
INCLUDE_DIRECTORIES(${PROTOBUF_INCLUDE_DIR})
PROTOBUF_GENERATE_CPP(PROTO_SRC PROTO_HEADER flux.proto)
ADD_LIBRARY(flux_proto ${PROTO_HEADER} ${PROTO_SRC})
set_property(TARGET flux_proto PROPERTY POSITION_INDEPENDENT_CODE ON)
